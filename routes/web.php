<?php

use App\Http\Controllers\Api\Admin\LoginController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

// Route::get('/admin/login', function () {
//     return view('auth.adminLogin'); // Menampilkan form login
// })->name('login'); // Nama rute harus 'login'

// Route::post('/admin/login', [LoginController::class, 'index'])->name('admin.login');

// Route::middleware('auth')->get('/admin/dashboard', function () {
//     return view('layouts.dashboard');
// })->name('dashboard');

// Route::post('/admin/logout', function () {
//     auth()->logout();
//     return redirect('/admin/login')->with('success', 'You have been logged out!');
// })->name('logout');

